// What the nav's "Currently sipping" block says. Shared with the footer, whose links sit under it:
// the second grid column is as wide as its content, so the footer reserves the same width (with a
// hidden copy of the longer line) to keep the two columns — and the links — lined up.
export const COFFEE_STATUS = {
  label: 'Currently sipping:',
  detail: 'Equalizer from Corvus Coffee Roasters',
}
